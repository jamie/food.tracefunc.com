<dl class="row">
  <% Array(items).each do |item| %>
    <% name, amount = item %>
    <dt class="col-sm-3 text-sm-right"><%= amount %></dt>
    <dd class="col-sm-9 ml-3 ml-sm-auto"><%= name %></dd>
  <% end %>
</dl>
