---
layout: default
title: ":prototype-term-titleize Recipes"
prototype:
  term: category
  collection: pages
---

<h1><%= page.data["title"] %></h1>

<div class="row">
  <% paginator.resources.sort_by{_1.data['title']}.each do |recipe| %>
    <div class="col-12 col-md-6 col-lg-4">
      <a href="<%= recipe.relative_url %>">
        <div class="card my-2">
          <div class="card-img-top d-none d-sm-block ratio ratio-16x9">
            <img style="object-fit: cover;" src="<%= recipe.data["image"] || "/images/placeholder.png" %>" alt="">
          </div>
          <div class="card-body">
            <h4 class="card-title text-center mb-0">
              <%= recipe.data["title"] %>
            </h4>
          </div>
        </div>
      </a>
    </div>
  <% end %>
</div>
